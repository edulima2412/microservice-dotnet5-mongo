version: '3.4'

services:
  catalog-api:
    container_name: catalog-api
    build:
      context: .
      dockerfile: ./src/Catalog.Api/Dockerfile
    ports:
      - "5000:80"
      - "5001:443"
    environment:
      - "ASPNETCORE_ENVIRONMENT=Development"